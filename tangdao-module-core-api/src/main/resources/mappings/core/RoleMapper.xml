<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tangdao.module.core.mapper.RoleMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        role_id as "roleId",
        role_name as "roleName",
        view_code as "viewCode",
        corp_id as "corpId",
        corp_name as "corpName",
		status as "status",
		create_by as "createBy",
		create_time as "createTime",
		update_by as "updateBy",
		update_time as "updateTime",
    	remarks as "remarks"
    </sql>

	<select id="listRoleInfo" resultType="com.tangdao.framework.model.RoleInfo">
		select 
			role_id as "roleId", 
			role_name as "roleName",
			view_code as "viewCode"
		from sys_role r, sys_user_role ur
		where r.role_id = ur.role_id and ur.user_id = #{userId}
	</select>
</mapper>
